<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-700 flex flex-wrap  gap-6 p-8">
        <% users.forEach(element => { %>
        <div class="bg-zinc-500 rounded-lg shadow-lg p-4 h-72 w-64 flex flex-col items-center text-black">
            <!-- Image -->
            <div class="w-56 h-40 overflow-hidden rounded-lg ">
                <img src="<%= element.img_url %>" alt="User Image" class="w-full h-full object-scale-down">
            </div>
    
            <!-- User Info -->
            <h1 class="text-lg font-bold mt-2 "><%= element.name %></h1>
            <p class="text-gray-300 text-sm "><%= element.email %></p>
            <p class="text-gray-300 text-sm mb-2"><%= element.phone %></p>
            <div class="mt-4 flex justify-between  w-full">
                <p class="text-white">  <a href="/edituser/<%= element._id  %>">Edit user </a></p>
                <p class="text-red-900"><a href="/deleteuser/<%= element._id  %>">Delete User</a></p>
            </div>
        </div>
        <% }) %>
    </div>
    
    
    
</body>
</html>